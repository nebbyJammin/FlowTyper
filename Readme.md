Flow.Launcher.Plugin.FlowTyper
==================

A plugin for the [Flow launcher](https://github.com/Flow-Launcher/Flow.Launcher) that adds a typing test, that measures accuracy and typing speed.

### Usage
In main mode, to start a typing test (typing mode):

    ft type

To return to main mode from typing mode, __press enter on the first result__